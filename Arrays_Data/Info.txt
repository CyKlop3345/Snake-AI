Data for AI with different layers size is collected here.
AI can work even with next layer sizes:
5-2-2-3

Input (first) and output (last) layer sizes is constant
Middle layers are hidden and may be changed

When use less layers size AI may do incorrect.
More hidden layers size is useless